# Voice Assistant Platform - Gateway Configuration

# Project Settings (Optional - defaults are in app/core/config.py)
# PROJECT_NAME="Voice Assistant Platform - Gateway"
# API_V1_STR="/v1"

# Redis Settings (MUST match across all services)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=

# Channels (Defaults are in app/core/config.py. Uncomment to override, ensure consistency with workers)
# AUDIO_STREAM_CHANNEL="audio_stream_channel"
# TRANSCRIPT_CHANNEL="transcript_channel"
# LLM_TOKEN_CHANNEL="llm_token_channel"
# LLM_TOOL_CALL_CHANNEL="llm_tool_call_channel"
# AUDIO_OUTPUT_STREAM_CHANNEL_PATTERN="audio_output_stream:{conversation_id}"

# Conversation Config Storage (Default is in app/core/config.py)
# CONVERSATION_CONFIG_PREFIX="conversation_config:"

# JWT Settings - IMPORTANT!
# Replace with a strong, unique secret key in your actual .env file
JWT_SECRET_KEY="your-super-secret-key-please-change-in-production-for-app-gateway"
# JWT_ALGORITHM="HS256" # Default, can be overridden
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440 # Default is 24 hours (1440 minutes)

# Logging Level (Optional - default is typically INFO or DEBUG in logging_config.py)
# LOG_LEVEL="INFO"